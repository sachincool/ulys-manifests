apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [../base]
# prod patches: tighten Linkerd policy to require mTLS for all in-namespace
# traffic; add a Linkerd default-deny Server with explicit AuthorizationPolicy
# allow-lists per service (already done in apps/base/worker/authorization.yaml).
